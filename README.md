# TP-Sequelize-Persona-Domicilio

- Comandos :
 1 - npm i
 2 - sequelize-cli db:migrate (tener previamente creada la base de datos con nombre "codeuno")
 3 - node server (para añadir datos a las tablas)
